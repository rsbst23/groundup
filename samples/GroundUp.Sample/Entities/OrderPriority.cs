namespace GroundUp.Sample.Entities;

/// <summary>
/// Order priority levels for testing enum filtering.
/// </summary>
public enum OrderPriority
{
    Low = 0,
    Normal = 1,
    High = 2,
    Critical = 3
}
